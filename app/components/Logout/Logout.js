import React, { PropTypes } from 'react'
import { subHeader } from 'styles/styles.css'

export default function Logout (props) {
  return (
    <div className={subHeader}>{'You are now logged out'}</div>
  )
}